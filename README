Linked list can be used as queue. The only difference from linear linked list is that the new element to be inserted is inserted as last node and when it comes to deletion, the first node is deleted because it was the first-in node.
